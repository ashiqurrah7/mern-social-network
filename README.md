# DevConnect

## Available Scripts

In the project directory, you can run:

### `npm run start`
### `npm run server`
### `npm run client`
### `npm run dev`

Client runs on port 3000 server runs on port 5000.